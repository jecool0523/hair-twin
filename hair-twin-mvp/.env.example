# Hair Twin MVP environment
#
# Fast MVP note:
# Vite exposes variables prefixed with VITE_ to browser code. That is acceptable
# only for local demos with a limited/test API key. For salon pilots or customer
# photos, move provider calls behind a backend/API route or worker and keep
# provider secrets off the frontend.

VITE_GEMINI_API_KEY=
